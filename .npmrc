; RoleEye is a public repository. Pin the public npm registry so a lockfile
; generated behind a corporate mirror never delegates dependency hosting for
; contributors or CI. See docs/security-review-phase1.md, finding 2.
registry=https://registry.npmjs.org/
